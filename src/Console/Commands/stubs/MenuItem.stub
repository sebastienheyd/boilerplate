<?php

namespace App\Menu;

use Sebastienheyd\Boilerplate\Menu\Builder;

class {{NAME}}
{
    public function make(Builder $menu)
    {
        $menu->add('My menu item', ['route' => 'boilerplate.home', 'permission' => 'backend', 'icon' => 'square'])
             ->id('boilerplate')
             ->activeIfRoute('boilerplate.*')
             ->order(100);
    }
}
